apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-logsense.fullname" . }}
  labels:
    {{- include "devops-logsense.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "devops-logsense.selectorLabels" . | nindent 4 }}
