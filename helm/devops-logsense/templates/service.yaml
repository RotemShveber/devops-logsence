apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-logsense.fullname" . }}
  labels:
{{ include "devops-logsense.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
{{ include "devops-logsense.selectorLabels" . | indent 4 }}
